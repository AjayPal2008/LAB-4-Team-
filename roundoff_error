# By submitting this assignment, I agree to the following:
#   "Aggies do not lie, cheat, or steal, or tolerate those who do."
#   "I have not given or received any unauthorized aid on this assignment."
#
# Names:        Micah Kadiri
#               Benjamin Hatch
#               Ajay Palanisamy
#               Hudson Dobbs
# Section:      508
# Assignment:   Lab Topic 4 (Team)
# Date:         11 September 2026
#
    
a = 1 / 7
print(f"a = {a}")
b = a * 7 
print(f"b = a * 7 = {b}")
# The value of b is rounded off to 1.0

c = 2 * a 
d = 5 * a
f = c + d
print(f"f = 2 * a + 5 * a = {f}")
# no the value of f is not 1.0 it is .999999999

from math import sqrt 
x = sqrt(1 / 3)
print(f"x = {x}")
y = x * x * 3
print(f"y = x * x * 3 = {y}")
z = x * 3 * x 
print(f"z = x * 3 * x = {z}")
# the value of y is 1.0
# the value of z is .99999

TOL = 1e-10
# check if b and f are equal within specified tolderance
if abs(b -f) < TOL:
    print(f"b and f are equal within tolderance of {TOL}")
else: 
    print(f"b and f are not equal with tolerance of {TOL}")
